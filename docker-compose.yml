services:
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: unix-password-cracker-ubuntu
    working_dir: /app
    volumes:
      - ./:/app
    stdin_open: true
    tty: true
    command: bash
